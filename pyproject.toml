[project]
name = "claude-plugins"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "django>=6.0.6",
    "playwright>=1.60.0",
]

[dependency-groups]
dev = [
    "ruff>=0.15.16",
]
